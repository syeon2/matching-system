spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/matching_system
    username: root
    password: 1234

  redis:
    # primary
    geo:
      host: localhost
      port: 6379
    # qualified
    routing-table:
      host: localhost
      port: 6380
    # qualified
    distributed-lock:
      host: localhost
      port: 6381
